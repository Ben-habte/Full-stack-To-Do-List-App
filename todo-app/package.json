{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "Full-stack Todo App - React + Node.js/Express + PostgreSQL. Built manually then rebuilt with AI assistance (Claude + Cursor) to compare development approaches.",
  "scripts": {
    "install-all": "cd server && npm install && cd ../client && npm install",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
